package com.feuch.clochette

// Target behaviour kept separate from runtime code for now:
// ContextRemarkEngine should merge context_lines.json and app_context_lines.json.
// This keeps the richer app-specific phrase library available without making the APK fragile.
