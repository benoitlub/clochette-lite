<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_clochette_background"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="12dp">

    <TextView
        android:id="@+id/widgetSprite"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/widget_clochette_sprite"
        android:gravity="center"
        android:text="🧚"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/widgetLine"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="5"
        android:text="Clochette attend sur l'écran d'accueil. C'est suspectement raisonnable."
        android:textColor="#2C183F"
        android:textSize="14sp"
        android:textStyle="bold" />
</LinearLayout>
